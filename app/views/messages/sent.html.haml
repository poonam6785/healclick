%legend 
  Sent Messages
  
  - unless @messages.blank?
    = link_to delete_all_sent_messages_path, method: :delete, confirm: 'Are you sure you want to delete all yours sent messages?', class: "pull-right btn btn-danger btn-xs" do
      .glyphicon.glyphicon-trash
      Delete all sent messages

= render partial: "messages_tabs"

.pull-right
  = paginate @messages

.clearfix

- if @messages.any?
  = render partial: "message", collection: @messages
- else
  .centered There are no sent messages

.pull-right
  = paginate @messages
  
.clearfix