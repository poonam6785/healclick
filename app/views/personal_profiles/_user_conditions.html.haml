- current_user.user_conditions.recent.each do |user_condition|  
  .well
    = link_to  user_condition_path(user_condition, format: :js), method: :delete, confirm: 'Are you sure you want to remove this condition?', class: "no-decoration", remote: true do
      %span.symptoms-remove-icon
    = user_condition.condition.try(:name)
  .clearfix