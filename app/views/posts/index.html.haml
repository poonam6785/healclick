%fieldset

  = render partial: 'shared/dashboard_top', locals: {title: 'My Topics'}

  .tab-content
    .tab-pane.active

      = render partial: "posts/posts_filter"

      = render partial: "posts/posts_sorter"

      #posts_container.scroll-pagination
        = render partial: 'posts_batch'

      .more_posts.centered{style: "margin: 20px;"}
        = hidden_field_tag :current_page, 1, id: :current_page
        = hidden_field_tag :content_type, 'my_posts', id: :content_type
        = hidden_field_tag :content_path, post_sorter_path(params[:sort_by]), id: :content_path

        .loading_container
          = image_tag 'loading.gif', size: "20x20"

#post_form_container
  = render partial: 'shared/post_form', locals: {post: @post}

#comment_form_container

#profile_popup