- if params[:filter].present?
  :plain
    $('#posts_container').html('#{j render(partial: 'posts/posts_batch')}');
    $('.treatment-summary-container .rating').html('#{j render('rating')}');
- else
  :plain
    $('#posts_container').append('#{j render(partial: 'posts/posts_batch')}');
:plain
  $('.loading_container').hide();
  load_useful_link_listeners();